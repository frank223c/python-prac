# Python propociona un conjunto estandad de operadores de comparacion binarios
# Menor que (<)
# Menor igual que (<=)
# Igual que (==)
# Diferente de (!=)
# Mayor que (>)
# Mayor igual que (>=)

a = 300
b = 300.00
c = -33
d = 'Python'
e = 'python'

print(a > b, a >= b, a == b, a != b, a < b, a <= b)
print(d < e, d <= e, d == e, d != e, d > e, d >= e)
print(-60 <= c < -40)

# print(c < e) no es permitido la comprarion de variables numericas con
# cadenas de caracteres